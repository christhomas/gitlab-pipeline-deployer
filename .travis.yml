sudo: required

services:
  - docker

language: generic

before_install:
  - TAG=$(time=$(date +%T) && echo "$(date +%F)_${time//:/.}")
  - docker build --pull -t pipeline-deployer:${TAG} .

script:
  - TAG=$(time=$(date +%T) && echo "$(date +%F)_${time//:/.}")
  - docker images pipeline-deployer:${TAG}